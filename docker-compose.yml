services:
  coordinator:
    build: .
    ports:
      - "8001:8001"
    env_file: .env
    depends_on:
      - redis
    command: uvicorn app.main:app --host 0.0.0.0 --port 8001

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
